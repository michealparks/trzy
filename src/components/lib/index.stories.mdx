
import { Canvas, Meta, Story } from '@storybook/blocks';

<Meta title='Lib' />

### Animation

Functions for more easily handling animations

```ts
import { fadeToAction } from 'trzy'

fadeToAction(lastAction, nextAction, duration, loop)
```

### Assets

Light abstractions on top of THREE loaders

```ts
import { setPath, loadAudio, loadFile, loadGLTF, loadTexture } from 'trzy'

setPath('path/to/assets')

const audio = await loadAudio('audio.mp3')
const file = await loadFile('file.json')
const gltf = await loadGLTF('cat.glb')
const texture = await loadTexture('dog.png')
```

### Audio

Additional audio helpers

```ts
import { preloadAudio, playAudio } from 'trzy'

preloadAudio('rickroll.mp3')
playAudio('rickroll.mp3', volume, loop)
```

### Resizing

Helpers for resizing the rendererer / effect composer

```ts
import { rendererResizer } from 'trzy'

const disposeResizer = rendererResizer(camera, renderer, composer, dpi)

```

### Storage

Localstorage but with JSON

```ts
import { save, load } from 'trzy'

save('bill', { type: 'mage', health: 0.25 })

const stats = load('bill')
```
