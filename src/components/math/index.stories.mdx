
import { Canvas, Meta, Story } from '@storybook/blocks';

<Meta title='Math' />

### Circle

```ts
import { randomPointOnCircle, randomPointWithinCircle } from 'trzy'

const [x, y] = randomPointOnCircle()

const [x, y] = randomPointWithinCircle()
```

### Sphere

```ts
import {randomPointOnSphere, randomPointWithinSphere } from 'trzy'

const [x, y, z] = randomPointOnSphere()

const [x, y, z] = randomPointWithinSphere()
```
